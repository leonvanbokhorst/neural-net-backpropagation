[project]
name = "neural-net-backpropagation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11,<3.13"
dependencies = [
    "numpy>=2.3.2",
    "torch>=2.8.0",
]
